<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
</head>

<body>
    <header>
        <button><a href="/user/logout">Log out</a></button>
    </header>
    <section>

        <% const ExpenseType = Expenses.ExpenseType; %>
        <% console.log(Expenses); %>

        <form action="/expense/editExpense/<%= Expenses._id %>" method="post">
            <input type="text" name="ExpenseType" value="<%= Expenses.ExpenseType %>">
            <input type="number" name="Amount" value="<%= Expenses.Amount %>">
            <input type="date" name="Date" value="<%= Date %>" id="">
            <button type="submit">submit</button>
        </form>
    </section>
</body>

</html>